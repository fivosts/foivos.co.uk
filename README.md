# foivos.co.uk

Static replacement for the former Wix site, designed to be served directly by GitHub Pages.

GitHub Pages should serve this repository from the `main` branch root. The custom domain is configured through `CNAME` as `foivos.co.uk`.
